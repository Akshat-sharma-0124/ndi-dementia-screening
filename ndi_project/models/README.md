# Model artifacts

`python -m src.train_classifier` writes `rf_model.pkl` here after real labeled WAV
files are available. The artifact bundles the fitted pipeline, ordered feature schema,
class names, training metrics, version, and creation time.

